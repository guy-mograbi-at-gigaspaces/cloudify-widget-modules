<project>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>
    <groupId>cloudify.widget</groupId>
    <artifactId>softlayer</artifactId>
    <version>1.0.0</version>
    <name>${project.artifactId}</name>

    <parent>
        <artifactId>modules</artifactId>
        <groupId>cloudify.widget</groupId>
        <version>1.0.0</version>
    </parent>

    <dependencies>
        <dependency>
            <groupId>cloudify.widget</groupId>
            <artifactId>modules</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>cloudify.widget</groupId>
            <artifactId>api</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.apache.jclouds</groupId>
            <artifactId>jclouds-core</artifactId>
            <version>${jCloudsVersion}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.jclouds</groupId>
            <artifactId>jclouds-compute</artifactId>
            <version>${jCloudsVersion}</version>
        </dependency>
        <dependency>
            <groupId>org.apache.jclouds.provider</groupId>
            <artifactId>softlayer-fixed</artifactId>
            <version>${jCloudsVersion}</version>
            <scope>system</scope>
            <systemPath>/home/eliranm/dev/cloudify-widget-modules/lib/softlayer-fixed-1.6.2-incubating.jar</systemPath>
        </dependency>
    </dependencies>

    <!--
<repositories>
&lt;!&ndash; We need this repo for the softlayer jclouds fix &ndash;&gt;
<repository>
    <id>my.mvn.repo</id>
    <url>https://github.com/iliapolo/maven-repo/raw/master</url>
</repository>
</repositories>
-->

</project>